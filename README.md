Para ejecutarlo se necesita el archivo db4o.jar, que se incluye en los archivos de este repositorio.

--Comandos--
En la terminal, usar javac -cp ".;db4o.jar" *.java para compilar todas las clases.
En la terminal, usar java -cp ".;db4o.jar" Main para ejecutar el archivo Main.java, utilizando db4o.jar
